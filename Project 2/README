Specific details in report in this directory.

Jongwoo Kim (503-787-749) Seasnet ID: kimj
Bryan Lau (404-015-053) Seasnet ID: bryanl

————————————————————

In bash, at the directory run "make" to create project executables

make - create runnable programs:
sender
    No parameters - lists default parameters
    -p X : specify port to run on
    -c X : specify corruption rate (0-100)
    -d X : specify fake drop rate (0-100)
    -w X : specify number of windows to use in GBN
receiver
    No parameters - lists default parameters
    -h X : specify host address
    -p X : specify port server is running on
    -f X : specify file to transmit

